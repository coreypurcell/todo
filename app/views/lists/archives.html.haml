%h2= @list.name
%p= @list.description

%h3 Archived To Do's
%ul#tasks
  = render :partial => 'tasks/archived_task', :collection => @list.tasks.archived

%hr
=link_to "Active To Do's", list_path(@list)
